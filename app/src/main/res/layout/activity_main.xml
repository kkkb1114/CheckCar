<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <include
        layout="@layout/actionbar_carbook"/>

    <FrameLayout
        android:id="@+id/frameLayout_activityMain"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

    </FrameLayout>

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigationView_activityMain"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        app:labelVisibilityMode="labeled"
        app:itemHorizontalTranslationEnabled="true"
        app:itemBackground="@color/bottomNavigationView_activityMainColor"
        app:itemTextColor="@drawable/bottomnavigation_menu_select_color"
        app:itemIconTint="@drawable/bottomnavigation_menu_select_color"
        app:menu="@menu/bottomnavigation_menu"/>

</LinearLayout>